# =====================================================================
# TrustCircle — Python Dependencies
# =====================================================================

# --- Core API Framework (Person 1) ---
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# --- Database (Person 1) ---
sqlalchemy>=2.0.30
psycopg2-binary>=2.9.9
alembic>=1.13.1

# --- Auth / JWT (Person 1) ---
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# --- Config / Validation (Person 1) ---
pydantic>=2.7.1
pydantic[email]>=2.7.1
pydantic-settings>=2.2.1

# --- HTTP Client ---
httpx>=0.27.0

# --- ML / Data (Person 2) ---
scikit-learn>=1.4.2
pandas>=2.2.2
numpy>=1.26.4

# --- Async / Queue (Person 3) ---
redis>=5.0.4
celery>=5.4.0

# --- WhatsApp / Twilio (Person 3) ---
twilio>=9.0.0

# --- Utilities ---
python-dotenv>=1.0.1

# --- Testing ---
pytest>=8.2.0
pytest-asyncio>=0.23.7
